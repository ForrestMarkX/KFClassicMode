class ClassicWeap_LMG_Stoner63A extends KFWeap_LMG_Stoner63A;

defaultproperties
{
    // Inventory / Grouping
    GroupPriority=180

    // DEFAULT_FIREMODE
    FireInterval(DEFAULT_FIREMODE)=+0.0857
    InstantHitDamage(DEFAULT_FIREMODE)=35.0
}
